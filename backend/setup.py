"""Setup script for Agentic RAG system."""
from setuptools import setup, find_packages

setup(
    name="agentic-rag",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.9",
)